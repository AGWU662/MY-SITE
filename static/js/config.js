/* ============================================
   ELITE WEALTH CAPITAL - Site Config
   Stub module — main configuration is in main.js
   ============================================ */
(function () {
    'use strict';

    // API base URL (relative so it works on any host)
    window.EWC_CONFIG = window.EWC_CONFIG || {
        API_BASE: '',
        API_URL: '/api',
        DEBUG: false,
        VERSION: '1.0.0',
    };
})();

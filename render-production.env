# ============================================================
# RENDER PRODUCTION ENVIRONMENT VARIABLES
# Elite Wealth Capital - elitewealthcapita.uk
# ============================================================
# 
# HOW TO USE THIS FILE:
# 1. Go to Render Dashboard → New Web Service
# 2. Connect GitHub repo: AGWU662/MY-SITE
# 3. Click "Environment" tab
# 4. Copy ALL variables below and paste into Render
# OR
# 5. Use "Import from .env" option in Render
#
# ============================================================

# Django Core Settings
SECRET_KEY=2b701cac9564763581c440415aae95ef
DEBUG=False
DJANGO_SETTINGS_MODULE=elite_wealth_capital.settings

# Allowed Hosts (comma-separated, no spaces)
ALLOWED_HOSTS=elitewealthcapita.uk,www.elitewealthcapita.uk,my-site-ghnp.onrender.com

# CSRF Trusted Origins (automatically generated from ALLOWED_HOSTS in settings.py)
# No need to set this separately - handled in settings.py

# Database Configuration (PostgreSQL - PERSISTENT)
# IMPORTANT: Use INTERNAL URL for faster connection
DATABASE_URL=postgresql://elite_admin:4nByWZVjPEQ2G1uIIki0vSCn99fuxKmw@dpg-d72e1uh4tr6s73bdvfp0-a/elite_wealth_k85f

# Email Configuration (Zoho SMTP)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.zoho.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=admin@elitewealthcapita.uk
EMAIL_HOST_PASSWORD=myfavour1$
DEFAULT_FROM_EMAIL=admin@elitewealthcapita.uk
ADMIN_EMAIL=admin@elitewealthcapita.uk

# Cloudinary Configuration (Cloud Storage for Images - REQUIRED)
# Sign up: https://cloudinary.com
CLOUDINARY_CLOUD_NAME=dh5ikf5un
CLOUDINARY_API_KEY=926391542761186
CLOUDINARY_API_SECRET=RGBNIYy2_c_wybiwH7-qINfbF5M

# Company Information
COMPANY_NAME=Elite Wealth Capital
COMPANY_EMAIL=admin@elitewealthcapita.uk
COMPANY_ADDRESS=London, United Kingdom, Norway
COMPANY_WEBSITE=https://elitewealthcapita.uk

# Tawk.to Live Chat (Optional - if you have these credentials)
# TAWK_PROPERTY_ID=your-property-id
# TAWK_WIDGET_ID=your-widget-id
# TAWK_API_KEY=your-api-key

# Redis (Optional - only if using Celery for background tasks)
# REDIS_URL=redis://localhost:6379/0

# ============================================================
# NOTES:
# ============================================================
# ✅ All sensitive data included (passwords, API keys, secrets)
# ✅ Cloudinary credentials added for image storage
# ✅ Database uses internal URL for better performance
# ✅ Production-ready settings (DEBUG=False)
# ✅ Email configured with Zoho SMTP
#
# IMPORTANT:
# - Keep this file PRIVATE (never commit to public GitHub)
# - When creating new Render service, paste all variables above
# - Update ALLOWED_HOSTS if using different domain
# - Database URL will stay same if using same PostgreSQL service
# ============================================================
